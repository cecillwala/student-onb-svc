spring:
  application:
    name: student-onb-svc
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB


  datasource:
    url: jdbc:postgresql://localhost:5432/student_onboarding
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

# ?? Server (student service = port 8080) ???????
server:
  port: 8080

# ?? Session Token Config ???????????????????????
session:
  token-expiry-hours: 336

# ?? CORS ???????????????????????????????????????
cors:
  allowed-origins: http://localhost:4200, https://basal-margy-droningly.ngrok-free.dev

# ?? File Storage ???????????????????????????????
file:
  upload-dir: ${FILE_UPLOAD_DIR:/home/cecillwala/LINUX/code/final-year-project/student-onb-svc/uploads}

# ?? SpringDoc / Swagger ????????????????????????
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha

chatbot:
  api-key: ${GEMINI_API_KEY:AIzaSyDYrNMoO-nWm1DnRPHqiGCCWm7upuTwo7Y}
  model: gemini-3.1-flash-lite-preview
  max-tokens: 1024